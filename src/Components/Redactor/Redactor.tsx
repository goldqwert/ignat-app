import React, { CSSProperties, useState } from 'react';
interface IProps {

}

const Redactor = (props: IProps) => {

    return (
        <div style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
        </div >
    )

};

export default Redactor;